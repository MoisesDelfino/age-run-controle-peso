RewriteEngine On
RewriteBase /controle/

# Serve arquivos reais diretamente
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Demais rotas vão para o roteador principal
RewriteRule ^ index.php [L,QSA]
